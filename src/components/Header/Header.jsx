import Heading from '../Heading/Heading';
import './Header.css';

const Header = () => {
  return (
    <header className='header'>
      <Heading />
      <button>theme</button>
    </header>
  );
};

export default Header;
